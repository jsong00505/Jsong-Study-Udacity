Q: What happens when you initialize a repository? Why do you need to do it?

A: I should initialize a repository, so I can use Git in the directory.

Q: How is the staging area different from the working directory and the repository? What value do you think it offers?

A: Staging area makes users manage their files trackable or untrackable. So users only track the files they want.

Q: How can you use the staging area to make sure you have one commit per logical change?

A: I can use the staging area by using the command 'git diff' and 'git diff --staged'. That command and option shows what I have changed.

Q: What are some situations when branches would be helpful in keeping your history organized? How would branches help?

A: When I want to add a new feature and the feature is not stable, I can use the branches. That is because new feature branch is not affect to my original branch.

Q: How do the diagrams help you visualize the branch structure?

A: I can see which one is original branch(I mean master) and when it merged. It is really helpful if the diagrams are visualized.

Q: What is the result of merging two branches together? Why do we represent it in the diagram the way we do?

A: The result is one master branch. The diagram makes us easy to understand.

Q: What are the pros and cons of Git’s automatic merging vs. always doing merges manually?

A: Pros is really easy to merge, and I don't need to compare all of my files changed. However cons is if the confilct is occured, I have to figure it out what problem is or have to talk to other branch's owner.